# Secrets
proxy.token
*.token
proxy.env

# Chrome user data
chrome-profile/

# Debug dumps
debug/

# Built binaries
search-service
search-proxy
search-relay
*.exe

# Search / download cache (data dir at repo root, not internal/cache)
/cache/

# Local API keys + engine priority (mode 0600). Never commit.
search-config.json
**/search-config.json
